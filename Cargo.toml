[package]
name = "pysegyfile"
version = "0.3.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "pysegyfile"
crate-type = ["cdylib"]

[dependencies]
pyo3 = "0.28"
numpy = "0.28"
segyfile = { path = "../segyfile"}
